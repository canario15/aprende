<h2>Informacion del <span>alumno</span></h2>
<%= form_for(:user, :url => registration_path(:user), :html => { class: 'contact-form' } ) do |f| %>
  <div class="row-fluid offset2">
    <div class="span4">
      <%= f.text_field :first_name, autofocus: true,  placeholder: "Nombre" %>
      <%= f.text_field :last_name, placeholder: "Apellido" %>
      <%= f.email_field :email, placeholder: "Mail" %>
      <%= f.password_field :password, placeholder: "Contraseña" %>
      <%= f.password_field :password_confirmation, placeholder: "Confirmar contraseña" %>
      <%= f.collection_select(:level_id, all_levels, :id, :title, { prompt: "Curso" }, { class: "placeholder" }) %>
      <%= f.collection_select(:institute_id, all_institutes, :id, :name, { prompt: "Instituto" }, { class: "placeholder" }) %>
    </div>
    <div class="span4">
      <output id="avatar_preview" >
        <span>
          <img class="default" src="/assets/default_user.png" >
        </span>
      </output>
      <%= f.file_field :avatar, accept: "image/*", class: "hidden",id: "avatar_field"  %>
      <%= render "cities/cities", f: f %>
    </div>
  </div>
  <div class="row-fluid margin">
    <div style="text-align:center">
      <button type="submit" class="btn btn-large">Confirmar</button>
    </div>
  </div>
<% end %>
