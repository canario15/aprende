<%= form_for @institute, :html => {:class => "contact-form"} do |f| %>
  <div class="row-fluid offset4">
    <div class="span4">
      <%= f.text_field :name, autofocus: true, class: 'form-control',  placeholder: "Nombre" %>
      <%=f.text_field :contact, class: 'form-control', placeholder: "Contacto" %>
      <%= f.text_field :phone, class: 'form-control', placeholder: "Teléfono" %>
      <%= f.email_field :email, class: 'form-control', placeholder: "Email" %>
    </div>
  </div>
  <div class="row-fluid margin">
    <div style="text-align:center">
      <%= link_to 'Cancelar', institutes_path, class: "btn btn-small" %>
      <%= f.button "Guardar", class: 'btn btn-small submit-buttons' %>
    </div>
  </div>
<% end %>