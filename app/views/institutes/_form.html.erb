<%= form_for @institute, :html => {:class => "contact-form"} do |f| %>
  <div class="row-fluid offset2">
    <div class="span4">
      <%= f.text_field :name, :autofocus => true, placeholder: 'Nombre' %>
      <%= f.text_field :contact, :autofocus => true, placeholder: 'Contacto' %>
      <%= f.text_field :phone, :autofocus => true, placeholder: 'Teléfono' %>
    </div>
    <div class="span4">
      <%= f.text_field :email, :autofocus => true, placeholder: 'Email'%>
      <%= render "cities/cities", f: f %>
    </div>
  </div>
  <div class="row-fluid margin">
    <div style="text-align:center">
      <%= link_to 'Cancelar', institutes_path ,class:"btn btn-large" %>
      <button type="submit" class="btn btn-large">Crear</button>
    </div>
  </div>
<% end -%>
