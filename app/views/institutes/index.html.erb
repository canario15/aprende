<!-- Institute Section -->
<div id="blog" class="section section-expand">
  <div class="section-title"><i class="icon-building"></i><strong>Institutos</strong>
    <p>para <span>enseñar</span></p>
  </div>

  <!-- Games Content -->
  <div class="container">
  </div>
</div>

<!--<div class="page-header">
  <h1 id="tables_institutes">Listado de Institutos</h1>
</div>
<%= link_to 'Nuevo Instituto', new_institute_path %>
<% unless @institutes.empty? -%>
  <table class="table table-striped table-bordered table-hover ">
    <thead>
      <tr>
        <th>Nombre</th>
        <th>Contacto</th>
        <th>Email</th>
        <th>Teléfono</th>
        <th>Ciudad</th>
      </tr>
    </thead>
    <tbody>
      <% @institutes.each do |institute| -%>
        <tr>
          <td><%= institute.name %></td>
          <td><%= institute.contact %></td>
          <td><%= institute.email %></td>
          <td><%= institute.phone %></td>
          <td><%= institute.city %></td>
          <td><%= link_to 'Editar', edit_institute_path(institute.id) %></td>
        </tr>
      <% end -%>
    </tbody>
  </table>
<% else -%>
  <p>No hay institutos</p>
<% end -%> -->