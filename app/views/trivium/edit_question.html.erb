<div class="page-header">
  <h1 id="tables">Editar Pregunta y Respuestas de Trivia: <%= @trivia.title %></h1>
</div>
<%= form_for @question, :url => update_question_trivia_path(@trivia.id, @question.id), :html => {:class => "bs-example form-horizontal"} do |f| %>
  <%= render 'form_question', f: f %>
<% end -%>
