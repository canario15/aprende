<div class="page-header">
  <h1 id="tables">Editar Pregunta y Respuestas de Trivia: <%= @trivia.title %></h1>
</div>
<%= form_for @question, :url => update_question_trivia_path(@trivia.id, @question.id), :html => {:class => "contact-form"} do |f| %>
  <%= render 'form_question', f: f %>
<% end -%>
