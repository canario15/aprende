Estadística de los juegos

<% @games.each do |g| %>
  Trivia: <%= g.trivia_title %>
    -Cantidad de Juegos: <%= g.count_games %>
    -Promedio de Puntos: <%= g.avg_score.to_f.round(0) %>
    <% result = g.count_answers_was_correct.to_f / g.count_answers.to_f%>
    <% result = result.nan? ? 0 : (100 * result).round(0) %>
    -Promedio de Preguntas Correctas: <%= result %>%

<% end -%>

