Estadística de los juegos

<% @games.each do |g| %>
  Trivia: <%= g.trivia.title %>
  <% t_id = g.trivia.id %>
    -Cantidad de Juegos: <%= @games_count[t_id] %>
    -Promedio de Puntos: <%= @games_avg[t_id].to_f.round(0) %>
    <% result = 100 * @answers_corrects_count[t_id].to_f / @answers_count[t_id].to_f%>
    <% result = result.nan? ? 0 : result %>
    -Promedio de Preguntas Correctas: <%= result.round(0) %>%

<% end -%>

